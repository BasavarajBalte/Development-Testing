spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  main:
    allow-bean-definition-overriding: true
  datasource:
    driver-class-name: org.h2.Driver 
    url: jdbc:h2:mem:testdemo;DB_CLOSE_DELAY=-1
    DB_CLOSE_DELAY: -1 
    username: majordomo 
    password: sa
  h2:
    console:
      enabled: true
